- 2026-03-21: Added Astro `i18n` config in `astro.config.mjs` with `defaultLocale: 'zh-cn'`, locales `['zh-cn', 'en']`, and `routing.prefixDefaultLocale: false` to keep default-language routes unprefixed.
- 2026-03-21: Added `src/i18n/ui.ts` with `languages`, `defaultLang`, and `ui` dictionaries (`zh-cn`, `en`) and added `src/i18n/utils.ts` with `getLangFromUrl`, `useTranslations`, and `useTranslatedPath` helpers for route-aware UI translation lookup.
- 2026-03-21: Restructured blog content into locale folders by moving all `src/content/blog/*.{md,mdx}` posts into `src/content/blog/en/` and creating `src/content/blog/zh-cn/hello-world.md` with Chinese frontmatter/content for localized routing.
- 2026-03-21: After moving posts into `en/`, updated relative asset references from `../../assets/...` to `../../../assets/...` in moved Markdown/MDX files so Astro content asset resolution still works at build time.
- 2026-03-21: Implemented explicit locale route split by keeping default locale pages in `src/pages/` (`/`, `/about`, `/blog`, `/blog/[...slug]`) and duplicating English routes under `src/pages/en/` (`/en`, `/en/about`, `/en/blog`, `/en/blog/[...slug]`) for compatibility with `prefixDefaultLocale: false`.
- 2026-03-21: Updated blog route generation to filter content IDs by locale prefix (`zh-cn/` in default routes, `en/` in `/en/*` routes) and strip the locale prefix when building URL slugs, ensuring clean URLs like `/blog/hello-world/` and `/en/blog/first-post/`.
- 2026-03-21: Added `src/components/LanguagePicker.astro` and updated `src/components/Header.astro` to use `getLangFromUrl`, `useTranslations`, and `useTranslatedPath`; navigation labels now come from `t('nav.*')`, nav URLs are locale-aware via `translatePath(...)`, and the language switcher is rendered in header actions.
